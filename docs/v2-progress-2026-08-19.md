# V2 Development Progress - 2026-08-19

## Completed Today

| Area | Status | Notes |
| --- | --- | --- |
| Branch setup | Done | Continued on `feat/v2-video-breakdown-workflow`; no remote push |
| V2 foundation | Done | Project/version/scene/workflow tables, Model Registry, KIE BYOK helper, server-side FFmpeg worker client, workflow APIs, Create/Projects UI, Generate handoff foundation |
| Real FFmpeg Worker app | Done | Added `workers/ffmpeg-worker` Node service, Dockerfile, worker package, bearer auth, ffprobe metadata, scene cut detection, long-scene segmentation, clip/keyframe/audio extraction, and R2 upload response contract |
| Structured scene analysis | Done | Added KIE chat JSON scene blueprint analysis with typed schema normalization and deterministic fallback when no key/API failure occurs |
| Scene failure isolation | Done | Video project can become ready even when individual scenes fall back/fail; scene status, error, and workflow job status capture the failure reason |
| Remix V2 | Done | Remix creates a new version and rewrites every scene through structured AI/fallback logic without overwriting Original |
| Single-scene edit | Done | Added `rewrite` API and UI for AI scene rewrite instructions |
| Scene retry | Done | Added `retry` API and UI for failed/fallback scene analysis |
| Generation metadata | Done | Added migration `0006_generation_workflow_links.sql`; generation jobs now store project/scene/projectVersion links and create `GENERATE_VIDEO` workflow jobs |
| Generated result callback path | Done | Status polling updates `video_generation`, `workflow_jobs`, and `scene_versions.generated_video_url` when a linked scene generation completes |
| Generate page handoff | Done | Dashboard passes projectId/sceneId/projectVersionId/duration into Generate; Generate submits those fields to `/api/generation-jobs` |
| Tests | Done | Added V2 workflow tests for fallback blueprint, remix fallback, and model routing |
| Daily docs | Done | Updated this progress chart and tomorrow development checklist |

## Validation

| Check | Result | Notes |
| --- | --- | --- |
| `pnpm exec tsc --noEmit` | Passed | TypeScript validation clean after V2 changes |
| `pnpm test -- --run` | Passed | 8 files, 66 tests passed |
| `pnpm lint` | Passed with warnings | 0 errors, 116 warnings; warnings are existing categories such as `any`, unused values, UI empty interface patterns |
| `pnpm build` | Passed | Next production build completed; emitted same lint warnings plus root detection, stale Browserslist, missing GitHub provider env warnings |

## Branch / Push State

| Item | State |
| --- | --- |
| Branch | `feat/v2-video-breakdown-workflow` |
| Local commits before this work | `17da618 Add V2 video breakdown workflow foundation` |
| Remote push | Not pushed |
| New DB migration | `drizzle/0006_generation_workflow_links.sql` |
| Worker app | `workers/ffmpeg-worker` |
| Required app env vars | `FFMPEG_WORKER_URL`, `FFMPEG_WORKER_SECRET`, `BYOK_ENCRYPTION_KEY`, `KIE_AI_API_KEY` or user KIE BYOK, optional `KIE_ANALYSIS_MODEL` / `KIE_ANALYSIS_ENDPOINT` |
| Required worker env vars | `WORKER_SECRET`, `R2_ENDPOINT`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET`, `R2_PUBLIC_URL` |

## Current Product State

```text
Reference Video Upload
  -> Project
  -> Cloud Run FFmpeg Worker breakdown contract
  -> Scene clips/keyframes/audio in R2
  -> Structured AI Video Blueprint per scene
  -> Editable scene prompt
  -> Retry failed scene analysis
  -> AI rewrite one scene
  -> Remix creates a new version
  -> Original vs Remix compare
  -> Send linked scene to Generate
  -> Persist generation task/project/scene/version
  -> Poll provider and write generated scene URL back
```

## Remaining Risk

| Risk | Impact | Mitigation |
| --- | --- | --- |
| Worker not smoke-tested against a real Cloud Run deployment in this session | Real media edge cases may still need tuning | Deploy worker and run a 6s + 30-60s reference-video smoke test |
| KIE scene analysis depends on user or env key | Without a KIE key, scenes fall back to editable deterministic blueprints | Add KIE key in Settings or `KIE_AI_API_KEY`; Retry scene after configuring |
| KIE provider model/API behavior is runtime-dependent | Generation may fail if provider credentials/model support differs | BYOK is passed to KIE; status errors are persisted in generation/workflow jobs |
| Existing lint warning debt remains | Does not block build, but hides future signal | Schedule a focused lint cleanup pass later |

